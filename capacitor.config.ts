import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.diana.analogaistudio',
  appName: 'Analog AI Studio',
  webDir: 'dist'
};

export default config;
